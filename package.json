{
  "private": true,
  "version": "0.0.0",
  "files": [
    "dist/",
    "code/"
  ],
  "dependencies": {
    "source-map-support": "^0.5.16"
  },
  "devDependencies": {
    "@rollup/plugin-json": "^4.0.2",
    "@rollup/plugin-node-resolve": "^7.1.3",
    "rollup": "^1.32.0",
    "rollup-plugin-jsy-lite": "^1.6.4"
  },
  "scripts": {
    "clean": "rm -rf ./dist/*",
    "build": "rollup --config",
    "watch": "npm -s run build -- --watch",
    "start": "node ./dist/test_astar.cjs"
  }
}
